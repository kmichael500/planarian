# react-scripts serves localhost over HTTPS when this is set. It generates a
# development certificate unless SSL_CRT_FILE and SSL_KEY_FILE are provided.
HTTPS=true
REACT_APP_API_ORIGIN_MAPPINGS='{"localhost":"https://localhost:7111"}'
